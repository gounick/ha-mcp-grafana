name: Prek

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  prek:
    name: Run prek hooks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run prek
        uses: j178/prek-action@v2
